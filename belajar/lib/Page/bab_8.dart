import 'package:flutter/material.dart';

class Bab8 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("placeholderr"),
          backgroundColor: Colors.grey[400],
        ),
        body: const Center(
          child: Text(
            'Bab 8 konten',
            style: TextStyle(fontSize: 35),
            ),
        ),
    );
  }
}
